# Production deployment for feedmate.ramadas.me — fully isolated from any
# other application on the host. No ports are published to the host at
# all: Postgres and the API are reachable only from other containers on
# the `feedmate_internal` network, and the API is additionally attached
# to the pre-existing `feedmate_public` external network so the host's
# shared Caddy reverse proxy (which belongs to a different, unrelated
# project) can reach it by container name — Caddy's own config and the
# other applications it serves are never touched by this file.
#
# Deploy with:
#   docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build
# Run migrations once (and after every migration-adding deploy) with:
#   docker compose -f docker-compose.prod.yml --env-file .env.prod run --rm migrate

services:
  postgres:
    image: postgres:16-alpine
    container_name: feedmate-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: feedmate
      POSTGRES_USER: feedmate_owner
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - feedmate_pgdata:/var/lib/postgresql/data
    networks:
      - feedmate_internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U feedmate_owner -d feedmate"]
      interval: 5s
      timeout: 5s
      retries: 10

  migrate:
    image: migrate/migrate:v4.17.1
    container_name: feedmate-migrate
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ../../db/migrations:/migrations
    networks:
      - feedmate_internal
    entrypoint:
      [
        "migrate",
        "-path", "/migrations",
        "-database", "postgres://feedmate_owner:${POSTGRES_PASSWORD}@postgres:5432/feedmate?sslmode=disable",
        "up"
      ]
    profiles: ["migrate"]

  # Service keys below are deliberately NOT "api"/"web" — Docker's embedded
  # DNS auto-aliases every container by its compose service key on every
  # network it joins, and the host's shared Caddy container is also joined
  # to a completely unrelated project's network whose services happen to
  # be named exactly "api" and "web". A multi-homed container's short-name
  # lookups aren't reliably scoped to one specific network, so a same-named
  # alias on ANY of Caddy's networks can shadow the intended target and
  # silently misroute traffic — this broke smartpay.ramadas.me in
  # production the first time these were named "api"/"web" here. The
  # Caddyfile always addresses these by their fully-qualified container_name
  # (feedmate-api / feedmate-web), which is unique host-wide, never by
  # these service keys — the rename below only changes the redundant alias.
  apisvc:
    build:
      context: ../../services/api
      dockerfile: Dockerfile
    container_name: feedmate-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      APP_ENV: production
      HTTP_ADDR: ":8081"
      DATABASE_URL: postgres://app_user:${APP_USER_PASSWORD}@postgres:5432/feedmate?sslmode=disable
      DATABASE_ADMIN_URL: postgres://app_admin:${APP_ADMIN_PASSWORD}@postgres:5432/feedmate?sslmode=disable
      JWT_SIGNING_KEY: ${JWT_SIGNING_KEY}
      SANDBOX_WEBHOOK_SECRET: ${SANDBOX_WEBHOOK_SECRET}
      LOG_LEVEL: info
    networks:
      - feedmate_internal
      - feedmate_public

  webapp:
    image: nginx:1.27-alpine
    container_name: feedmate-web
    restart: unless-stopped
    volumes:
      - ./web:/usr/share/nginx/html:ro
    networks:
      - feedmate_public

networks:
  feedmate_internal:
    name: feedmate_internal
  feedmate_public:
    name: feedmate_public
    external: true

volumes:
  feedmate_pgdata:
    name: feedmate_pgdata
